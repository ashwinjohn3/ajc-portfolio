---
interface Photo {
  src: string
  alt: string
  width: number
  height: number
}

interface Props {
  photos: Photo[]
}

const { photos } = Astro.props
---

<div class="photo-grid-simple">
  {photos.map((photo) => (
    <figure class="photo-cell">
      <img
        src={photo.src}
        alt={photo.alt}
        width={photo.width}
        height={photo.height}
        loading="lazy"
        decoding="async"
        class="h-full w-full object-cover"
      />
    </figure>
  ))}
</div>
