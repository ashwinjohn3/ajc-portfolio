---
import type { CollectionEntry } from 'astro:content'
import BaseLayout from './BaseLayout.astro'
import PageSection from '../components/PageSection.astro'

interface Props {
  title: string
  label: string
  activePath: '/projects' | '/photography'
  entry: CollectionEntry<'projects'> | CollectionEntry<'photography'>
}

const { title, label, activePath, entry } = Astro.props
---

<BaseLayout title={title}>
  <PageSection label={label} activePath={activePath}>
    <article>
      <header class="border-b border-rule pb-4 dark:border-night-rule">
        <h1 class="content-title">{entry.data.title}</h1>
      </header>
      <slot />
    </article>
  </PageSection>
</BaseLayout>
